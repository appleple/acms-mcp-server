/**
 * リソース登録モジュール
 * すべてのリソースを一括で登録する
 */

import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

/**
 * すべてのリソースをサーバーに登録
 */
export function registerAllResources(server: McpServer) {
  // registerEntryResource(server);
}
